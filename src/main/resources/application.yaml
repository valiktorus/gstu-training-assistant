spring:
  datasource:
    username: root
    password: example
    url: jdbc:mysql://localhost:33061
  jpa:
    show-sql: true
server:
  port: 8081
service:
  default-sorting-value: По умолчанию
  default-filtration-value: Не выбрано

